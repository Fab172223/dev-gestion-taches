spring:
  application:
    name: taskManagement
  jpa:
    show-sql: true
  flyway:
    enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/task_management
    username: postgres
    password: postgres
    spring.jpa.hibernate.ddl-auto: validate